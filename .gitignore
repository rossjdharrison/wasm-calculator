# dependencies
node_modules/

# build output (regenerate with `npm run asbuild` / `npm run build`)
build/
dist/

# wrangler / cloudflare
.wrangler/

# logs & OS cruft
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# local editor/agent settings
.claude/settings.local.json

# model imagery — served locally in dev; to be hosted on Cloudflare R2 in
# production (kept out of git history). The per-folder README documents filenames.
web/cars/*.png
web/cars/*.jpg
web/cars/*.jpeg
web/cars/*.webp
web/cars/*.avif
web/antiques/*.png
web/antiques/*.jpg
web/antiques/*.jpeg
web/antiques/*.webp
web/antiques/*.avif

# generated KV seed (npm run seed:kv)
kv-seed.json

# local Pages secrets (wrangler pages dev)
.dev.vars
