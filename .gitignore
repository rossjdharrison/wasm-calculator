# dependencies
node_modules/

# build output (regenerate with `npm run asbuild` / `npm run build`)
build/
dist/

# wrangler / cloudflare
.wrangler/

# logs & OS cruft
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# local editor/agent settings
.claude/settings.local.json

# car images — served locally in dev; to be hosted on Cloudflare R2 in production
# (kept out of git history). web/cars/README.md documents the expected filenames.
web/cars/*.png
web/cars/*.jpg
web/cars/*.jpeg
web/cars/*.webp
web/cars/*.avif
